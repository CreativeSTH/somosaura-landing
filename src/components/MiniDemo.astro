---
import Icon from './Icon.astro';

const products = [
  { name: 'Gaseosa', price: 8500, icon: 'cup-soda' },
  { name: 'Pan', price: 4200, icon: 'wheat' },
  { name: 'Combo', price: 15000, icon: 'package' },
];
---

<div class="mini-demo" id="miniDemo">
  <p class="mini-demo-title">Probalo — creá una venta real</p>
  <div class="mini-products">
    {products.map((p) => (
      <button type="button" class="mini-product" data-price={p.price}>
        <span class="mini-product-icon"><Icon name={p.icon} size={20} /></span>
        <span class="mini-product-name">{p.name}</span>
        <span class="mini-product-price">${p.price.toLocaleString('es-CO')}</span>
      </button>
    ))}
  </div>
  <div class="mini-cart-row">
    <span id="miniCartCount">0 items</span>
    <span id="miniCartTotal">$0</span>
  </div>
  <button type="button" class="mini-cobrar" id="miniCobrarBtn">Cobrar</button>

  <!-- Diálogo 1: cuánto recibe y cuánto devuelve -->
  <div class="mini-overlay mini-pay-dialog" id="miniPayDialog">
    <p class="mini-dialog-eyebrow">Total a cobrar</p>
    <p class="mini-dialog-total" id="miniPayTotal">$0</p>

    <label class="mini-dialog-label" for="miniPayReceived">Recibe</label>
    <input type="number" inputmode="numeric" class="mini-dialog-input" id="miniPayReceived" placeholder="$0" />

    <div class="mini-dialog-change-row">
      <span>Devuelta</span>
      <span id="miniPayChange">$0</span>
    </div>

    <div class="mini-dialog-actions">
      <button type="button" class="mini-dialog-cancel" id="miniPayCancel">Cancelar</button>
      <button type="button" class="mini-dialog-confirm" id="miniPayConfirm">Pagar</button>
    </div>
  </div>

  <!-- Diálogo 2: imprimiendo factura -->
  <div class="mini-overlay mini-print-dialog" id="miniPrintDialog">
    <span class="mini-spinner"></span>
    <p>Imprimiendo factura…</p>
  </div>

  <!-- Alerta final -->
  <div class="mini-overlay mini-alert-dialog" id="miniAlertDialog">
    <span class="mini-alert-icon"><Icon name="check-circle" size={30} /></span>
    <p class="mini-alert-title">Venta finalizada</p>
    <p class="mini-alert-sub">2 clicks, cero pantallas nuevas.</p>
  </div>
</div>

<script>
  import { formatCOP } from '../lib/formatCurrency';
  import { createCart } from '../lib/miniDemoCart';
  import { computeChange, suggestReceived } from '../lib/payment';

  const root = document.getElementById('miniDemo');
  if (root) {
    const cart = createCart();
    const countEl = document.getElementById('miniCartCount')!;
    const totalEl = document.getElementById('miniCartTotal')!;

    const payDialog = document.getElementById('miniPayDialog')!;
    const payTotalEl = document.getElementById('miniPayTotal')!;
    const receivedInput = document.getElementById('miniPayReceived') as HTMLInputElement;
    const changeEl = document.getElementById('miniPayChange')!;
    const confirmBtn = document.getElementById('miniPayConfirm') as HTMLButtonElement;

    const printDialog = document.getElementById('miniPrintDialog')!;
    const alertDialog = document.getElementById('miniAlertDialog')!;

    function render() {
      countEl.textContent = `${cart.items.length} items`;
      totalEl.textContent = formatCOP(cart.total());
    }

    function updateChange() {
      const total = cart.total();
      const received = Number(receivedInput.value) || 0;
      const change = computeChange(total, received);
      changeEl.textContent = formatCOP(change);
      confirmBtn.disabled = received < total;
    }

    root.querySelectorAll<HTMLButtonElement>('.mini-product').forEach((btn) => {
      btn.addEventListener('click', () => {
        btn.classList.add('added');
        cart.add(Number(btn.dataset.price));
        render();
      });
    });

    document.getElementById('miniCobrarBtn')?.addEventListener('click', () => {
      if (cart.items.length === 0) return;
      const total = cart.total();
      payTotalEl.textContent = formatCOP(total);
      receivedInput.value = String(suggestReceived(total));
      updateChange();
      payDialog.classList.add('show');
    });

    receivedInput.addEventListener('input', updateChange);

    document.getElementById('miniPayCancel')?.addEventListener('click', () => {
      payDialog.classList.remove('show');
    });

    confirmBtn.addEventListener('click', () => {
      payDialog.classList.remove('show');
      printDialog.classList.add('show');

      setTimeout(() => {
        printDialog.classList.remove('show');
        alertDialog.classList.add('show');

        setTimeout(() => {
          alertDialog.classList.remove('show');
          cart.clear();
          root.querySelectorAll('.mini-product.added').forEach((el) => el.classList.remove('added'));
          render();
        }, 1800);
      }, 1600);
    });
  }
</script>

<style>
  .mini-demo {
    position: relative;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 14px;
    padding: 18px;
    width: 100%;
    max-width: 320px;
  }

  @media (min-width: 900px) {
    .mini-demo {
      max-width: 400px;
      padding: 24px;
    }
  }
  .mini-demo-title {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--accent-violet);
    margin: 0 0 12px;
    font-weight: 700;
  }
  .mini-products {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
    padding: 6px 2px;
  }
  .mini-product {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    background: linear-gradient(155deg, rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.02));
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 14px;
    padding: 12px 6px 10px;
    text-align: center;
    cursor: pointer;
    font-size: 11px;
    color: var(--fg);
    font-weight: 600;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    transition: transform 0.15s var(--ease-out), border-color 0.15s;
  }
  .mini-product-icon {
    display: flex;
    color: var(--accent-violet);
    margin-bottom: 4px;
  }
  .mini-product-name {
    color: var(--fg-muted);
  }
  .mini-product-price {
    font-size: 13px;
    font-weight: 800;
    color: var(--accent-violet);
    margin-top: 2px;
  }
  /* Desordenadas a propósito, como un mazo de tarjetas tirado sobre la mesa. */
  .mini-products > .mini-product:nth-child(1) {
    transform: rotate(-5deg) translateY(4px);
  }
  .mini-products > .mini-product:nth-child(2) {
    transform: rotate(4deg) translateY(-6px);
  }
  .mini-products > .mini-product:nth-child(3) {
    transform: rotate(-3deg) translateY(5px);
  }
  .mini-product:hover {
    transform: translateY(-4px) scale(1.05) rotate(0deg);
    border-color: var(--accent-violet);
  }
  .mini-product.added {
    border-color: var(--accent-violet);
    background: rgba(150, 110, 255, 0.18);
  }
  .mini-cart-row {
    display: flex;
    justify-content: space-between;
    font-size: 12px;
    color: var(--fg-muted);
    margin-bottom: 12px;
  }
  .mini-cobrar {
    width: 100%;
    background: var(--fg);
    border: none;
    color: #0a0612;
    font-weight: 700;
    padding: 12px;
    border-radius: var(--radius-pill);
    cursor: pointer;
    font-size: 13px;
  }

  /* Los tres diálogos comparten la misma base: cubren la tarjeta entera. */
  .mini-overlay {
    position: absolute;
    inset: 0;
    background: rgba(8, 6, 12, 0.94);
    backdrop-filter: blur(6px);
    border-radius: 14px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 22px;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s var(--ease-out);
  }
  .mini-overlay.show {
    opacity: 1;
    pointer-events: auto;
  }

  .mini-dialog-eyebrow {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--fg-muted);
    margin: 0 0 4px;
  }
  .mini-dialog-total {
    font-size: 26px;
    font-weight: 800;
    margin: 0 0 18px;
  }
  .mini-dialog-label {
    align-self: flex-start;
    font-size: 12px;
    color: var(--fg-muted);
    margin-bottom: 6px;
  }
  .mini-dialog-input {
    width: 100%;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 10px;
    padding: 10px 12px;
    color: var(--fg);
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 14px;
  }
  .mini-dialog-input:focus {
    outline: none;
    border-color: var(--accent-violet);
  }
  .mini-dialog-change-row {
    width: 100%;
    display: flex;
    justify-content: space-between;
    font-size: 13px;
    color: var(--fg-muted);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 12px;
    margin-bottom: 18px;
  }
  .mini-dialog-change-row span:last-child {
    color: #8ff0ac;
    font-weight: 800;
    font-size: 15px;
  }
  .mini-dialog-actions {
    display: flex;
    gap: 10px;
    width: 100%;
  }
  .mini-dialog-cancel {
    flex: 1;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.18);
    color: var(--fg);
    font-weight: 600;
    padding: 11px;
    border-radius: var(--radius-pill);
    cursor: pointer;
    font-size: 13px;
  }
  .mini-dialog-confirm {
    flex: 1;
    background: var(--accent-violet);
    border: none;
    color: #0a0612;
    font-weight: 700;
    padding: 11px;
    border-radius: var(--radius-pill);
    cursor: pointer;
    font-size: 13px;
  }
  .mini-dialog-confirm:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .mini-spinner {
    width: 34px;
    height: 34px;
    border: 3px solid rgba(255, 255, 255, 0.15);
    border-top-color: var(--accent-violet);
    border-radius: 50%;
    animation: miniSpin 0.8s linear infinite;
    margin-bottom: 14px;
  }
  @keyframes miniSpin {
    to {
      transform: rotate(360deg);
    }
  }
  .mini-print-dialog p {
    font-size: 13px;
    color: var(--fg-muted);
    margin: 0;
  }

  .mini-alert-icon {
    display: flex;
    color: #8ff0ac;
    margin-bottom: 10px;
  }
  .mini-alert-title {
    font-size: 16px;
    font-weight: 800;
    margin: 0 0 4px;
  }
  .mini-alert-sub {
    font-size: 12px;
    color: var(--fg-muted);
    margin: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .mini-spinner {
      animation-duration: 1.6s;
    }
  }

  @media (max-width: 400px) {
    .mini-products {
      flex-direction: column;
    }
  }
</style>
